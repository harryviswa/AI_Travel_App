# Environment Variables Template
# Copy this file to .env and fill in your actual API keys

# Server Port
PORT=5000

# Provider API Keys (non-Google stack)
# OpenCage Geocoding (search)
OPENCAGE_API_KEY=your_opencage_api_key_here
# OpenRouteService (directions + matrix)
ORS_API_KEY=your_openrouteservice_api_key_here
# OpenTripMap (nearby POIs) - OPTIONAL: Leave blank to use IP-based rate limiting (100 req/day)
# Get key from https://opentripmap.io/profile (requires GitHub login for 1000 req/day)
OPENTRIPMAP_API_KEY=

# AI Configuration
# AI_PROVIDER: 'ollama' (default, local) or 'openai' (cloud)
AI_PROVIDER=ollama

# Ollama Configuration (for local LLM)
# Make sure Ollama is running locally: ollama serve
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# OpenAI API Key (Optional - only if using AI_PROVIDER=openai)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
